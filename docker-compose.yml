services:
  recorder:
    build: .
    ports:
      - "5000:5000"
    environment:
      CAPTURE_DIR: /app/captures
      MAX_CONCURRENT_STREAMS: 5
    volumes:
      - ./captures:/app/captures
    restart: unless-stopped
